# Replace invisible characters
#### Replaces non-breaking control characters and other control characters with spaces.

I mainly use this to strip invisible characters from AI output and replace them with valid spaces. Please refer to the example files `input.py` and `output.py`.